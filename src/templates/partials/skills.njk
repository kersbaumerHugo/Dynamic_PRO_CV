<h2>Core Skills</h2>
{% if profile.featuredSkills and profile.featuredSkills.length %}
  <ul class="skills-list">
  {% for skill in profile.featuredSkills %}
    <li>{{ skill.label[locale] || skill.label || skill.id }}</li>
  {% endfor %}
  </ul>
{% else %}
  <p>No skills selected.</p>
{% endif %}
